//@triplett/stew@v3.4.1
(() => {
	// src/document.js
	var selfClosingTags = /* @__PURE__ */ new Set([
	  "wbr",
	  "track",
	  "source",
	  "param",
	  "meta",
	  "link",
	  "keygen",
	  "input",
	  "img",
	  "hr",
	  "embed",
	  "command",
	  "col",
	  "br",
	  "base",
	  "area",
	  "!doctype"
	]);
	var nameMap = {
	  className: "class",
	  tabIndex: "tabindex"
	};
	function findMatches(nodes, selectors, matches) {
	  for (const node of nodes) {
		if (!("tagName" in node)) {
		  continue;
		}
		const nodeClasses = new Set((node.className || "").trim().split(/\s+/));
		for (const [query, ...childQueries] of selectors) {
		  const [tagName, id, ...classes] = query;
		  let childSelectors = selectors;
		  const isMatch = (!tagName || tagName === node.tagName) && (!id || id === node.id) && classes.every((item) => nodeClasses.has(item));
		  if (isMatch) {
			if (!matches) {
			  return node;
			} else if (!childQueries.length) {
			  matches.add(node);
			} else {
			  childSelectors = [childQueries, ...childSelectors];
			}
		  }
		  const childNode = findMatches(node.childNodes, childSelectors, matches);
		  if (childNode) {
			return childNode;
		  }
		}
	  }
	}
	function parseSelector(selector) {
	  return selector.trim().split(/\s*,\s*/).map((selector2) => {
		return selector2.split(/\s+/).map((level) => {
		  const items = level.split(".");
		  const [tagName, id = ""] = items.shift().split("#");
		  items.unshift(tagName, id);
		  return items;
		});
	  });
	}
	function writeChildNodes(childNodes, tagName) {
	  const allChildNodes = [];
	  let wasTextNode = false;
	  for (const node of childNodes) {
		const isTextNode = "nodeValue" in node;
		if (wasTextNode && isTextNode)
		  allChildNodes.push("<!---->");
		allChildNodes.push(node);
		wasTextNode = isTextNode;
	  }
	  if (tagName === "style" || tagName === "script") {
		const [firstChild = ""] = allChildNodes;
		return firstChild.nodeValue.replace(/<\//, "&lt;/");
	  }
	  return allChildNodes.join("");
	}
	var virtualDocument = {
	  createTextNode(nodeValue) {
		return {
		  nodeValue,
		  toString() {
			return this.nodeValue.replace(/\&/, "&amp;").replace(/</, "&lt;").replace(/>/g, "&gt;");
		  }
		};
	  },
	  createDocumentFragment() {
		return {
		  childNodes: [],
		  appendChild(child) {
			this.removeChild(child);
			this.childNodes.push(child);
		  },
		  insertBefore(child, sibling) {
			const { childNodes } = this;
			this.removeChild(child);
			const index = childNodes.indexOf(sibling);
			childNodes.splice(index, 0, child);
		  },
		  removeChild(child) {
			const { childNodes } = this;
			const index = childNodes.indexOf(child);
			if (index !== -1) {
			  childNodes.splice(index, 1);
			}
		  },
		  querySelector(selector) {
			const selectors = parseSelector(selector);
			return findMatches(this.childNodes, selectors) || null;
		  },
		  querySelectorAll(selector) {
			const selectors = parseSelector(selector);
			const matches = /* @__PURE__ */ new Set();
			findMatches(this.childNodes, selectors, matches);
			return [...matches];
		  },
		  toString() {
			const { childNodes } = this;
			return writeChildNodes(childNodes);
		  }
		};
	  },
	  createElement(tagName) {
		return {
		  tagName: tagName.toUpperCase(),
		  style: {},
		  dataset: {},
		  mode: null,
		  ...this.createDocumentFragment(),
		  toString() {
			const { tagName: tagName2, childNodes, style, dataset, ...rest } = this;
			const attributeEntries = Object.entries(rest);
			const styleEntries = Object.entries(style);
			const datasetEntries = Object.entries(dataset);
			const lowercaseTagName = tagName2.toLowerCase();
			let html = `<${lowercaseTagName === "!doctype" ? "!DOCTYPE" : lowercaseTagName}`;
			let content = writeChildNodes(childNodes, tagName2);
			if (html === "<!doctype") {
			  html = "<!DOCTYPE";
			}
			if (isServer) {
			  attributeEntries.sort(([a], [b]) => a.localeCompare(b));
			  styleEntries.sort(([a], [b]) => a.localeCompare(b));
			  datasetEntries.sort(([a], [b]) => a.localeCompare(b));
			}
			for (let [name, value] of attributeEntries) {
			  if (!value && value !== 0 || typeof value === "function" || /['"&<>]/.test(name)) {
				continue;
			  }
			  name = nameMap[name] || name.replace(/(?=[A-Z])/g, "-").toLowerCase();
			  html += ` ${name}${value === true ? "" : `="${String(value).replace(/"/g, "&quot;")}"`}`;
			}
			const styleString = styleEntries.map(([name, value]) => {
			  return `${name.replace(/(?=[A-Z])/g, "-").toLowerCase()}:${value};`;
			}).join("");
			if (styleString)
			  html += ` style="${styleString}"`;
			for (const [name, value] of datasetEntries) {
			  html += ` data-${name}${value === true ? "" : `="${String(value).replace(/"/g, "&quot;")}"`}`;
			}
			if (this.mode !== null) {
			  content = `<template shadowrootmode="${this.mode}">${content}</template>`;
			}
			if (selfClosingTags.has(lowercaseTagName)) {
			  return `${html}>`;
			}
			return `${html}>${content}</${lowercaseTagName}>`;
		  }
		};
	  }
	};
	var isServer = typeof window !== "object";
  
	// src/element.js
	function renderElement(ref, props, children, context, document, nodes) {
	  let [tagName, memo, node] = ref;
	  if (!node && tagName !== "") {
		const { shadowrootmode } = props;
		if (typeof shadowrootmode === "boolean" && tagName?.toUpperCase?.() === "TEMPLATE") {
		  node = parentNode.shadowRoot || parentNode.attachShadow({ mode: shadowrootmode });
		} else {
		  node = document.createElement(typeof tagName === "number" ? `h${tagName}` : tagName);
		}
		ref[2] = node;
	  }
	  if (node) {
		for (const [name, value] of Object.entries(props)) {
		  switch (name) {
			case "style":
			case "dataset": {
			  const object = node[name];
			  for (const [name2, string] of Object.entries(value)) {
				if (string !== object[name2]) {
				  object[name2] = string;
				}
			  }
			  continue;
			}
			case "ref": {
			  value?.push?.(node);
			  continue;
			}
		  }
		  if (value !== node[name]) {
			node[name] = value;
		  }
		}
	  } else {
		context = { ...context, ...props };
	  }
	  const map = {};
	  const removeRefs = new Set(ref.slice(3));
	  const nextNodes = [node];
	  for (const [i, childLayout] of children.entries()) {
		const childRef = render(childLayout, context, document, nextNodes, ref, i, map);
		if (childRef) {
		  removeRefs.delete(childRef);
		} else if (memo === void 0) {
		  ref.splice(i + 3, 0, void 0);
		}
	  }
	  for (const childRef of removeRefs) {
		remove(childRef, node);
	  }
	  ref[1] = Object.keys(map).length ? map : null;
	  ref.splice(children.length + 3);
	  nextNodes.shift();
	  if (!node) {
		nodes.push(...nextNodes);
		return;
	  }
	  reconcile(node, nextNodes, [...node.childNodes]);
	  if (tagName !== node) {
		nodes.push(node);
	  }
	}
  
	// src/state.js
	var queue = /* @__PURE__ */ new Set();
	var scheduleQueue = typeof requestAnimationFrame !== "undefined" ? requestAnimationFrame : setTimeout;
	function unsubscribe(impulse) {
	  const subscriptions = impulse[1];
	  for (const subscription of subscriptions) {
		subscription.delete(impulse);
	  }
	  subscriptions.clear();
	}
	function schedule(subscriptions) {
	  if (!subscriptions.size) {
		return;
	  } else if (!queue.size) {
		scheduleQueue(() => {
		  for (const impulse of queue) {
			const [update, , , ...parentImpulses] = impulse;
			unsubscribe(impulse);
			if (!parentImpulses.some((parentImpulse) => queue.has(parentImpulse))) {
			  update();
			}
		  }
		  queue.clear();
		  processEffects();
		}, 0);
	  }
	  for (const impulse of subscriptions) {
		queue.add(impulse);
	  }
	}
	function createState(state) {
	  if (isServer) {
		return state;
	  }
	  for (const name in state) {
		const subscriptions = /* @__PURE__ */ new Set();
		let value = state[name];
		Object.defineProperty(state, name, {
		  get() {
			if (impulses.length) {
			  const [impulse] = impulses;
			  subscriptions.add(impulse);
			  impulse[1].add(subscriptions);
			}
			return value;
		  },
		  set(newValue) {
			if (newValue !== value) {
			  value = newValue;
			  schedule(subscriptions);
			}
		  }
		});
	  }
	  return state;
	}
  
	// src/view.js
	function remove(ref, parentNode2) {
	  if (!Array.isArray(ref)) {
		if (ref && parentNode2) {
		  parentNode2.removeChild(ref);
		}
		return;
	  }
	  const [, , node, ...children] = ref;
	  if (Array.isArray(node)) {
		const [, subscriptions, proxy] = node;
		unsubscribe(subscriptions);
		remove(proxy, node);
		for (const [teardown] of children) {
		  execute(teardown);
		}
	  } else {
		if (node && parentNode2) {
		  parentNode2.removeChild(node);
		  parentNode2 = void 0;
		}
		for (const childRef of children) {
		  remove(childRef, parentNode2);
		}
	  }
	}
	function reconcile(node, nextNodes, prevNodes) {
	  let nodeIndex = prevNodes.length - 1;
	  let prevNode = prevNodes[nodeIndex];
	  let sibling;
	  for (let i = nextNodes.length - 1; i >= 0; i--) {
		const nextNode = nextNodes[i];
		if (nextNode === prevNode) {
		  nodeIndex--;
		  prevNode = prevNodes[nodeIndex];
		} else if (sibling) {
		  node.insertBefore(nextNode, sibling);
		} else {
		  node.appendChild(nextNode);
		}
		sibling = nextNode;
	  }
	}
	function render(layout, context, document, nodes, container, i, map) {
	  let ref = container[i + 3] || [];
	  if (!Array.isArray(layout)) {
		switch (typeof layout) {
		  default: {
			ref = void 0;
			break;
		  }
		  case "number": {
			layout = String(layout);
		  }
		  case "string": {
			if (ref.nodeValue === void 0) {
			  ref = document.createTextNode(layout);
			} else if (layout !== ref.nodeValue) {
			  ref.nodeValue = layout;
			}
			nodes.push(ref);
			break;
		  }
		  case "object": {
			const { "": convert } = context;
			layout = [convert, layout];
			return render(layout, context, document, nodes, container, i, map);
		  }
		  case "function": {
			layout = layout({ ...context, "": i });
			return render(layout, context, document, nodes, container, i, map);
		  }
		}
	  } else {
		let [tagName, object, ...children] = layout;
		const { "": key, ...props } = object || {};
		let callback = renderElement;
		let node;
		ref = container[1]?.[key] || ref;
		switch (typeof tagName) {
		  case "object": {
			if (tagName) {
			  node = tagName;
			  nodes = [];
			  break;
			}
		  }
		  case "undefined":
		  case "boolean": {
			tagName = "";
			break;
		  }
		  case "function": {
			callback = renderImpulse;
			break;
		  }
		}
		if (tagName !== ref[0]) {
		  if (!node && ref.tagName && tagName.toUpperCase() === ref.tagName) {
			ref = [tagName, , ref, ...ref.childNodes];
		  } else {
			ref = [tagName, , node];
		  }
		}
		if (key) {
		  map[key] = ref;
		}
		callback(ref, props, children, context, document, nodes, container, i);
	  }
	  return container[i + 3] = ref;
	}
  
	// src/impulse.js
	var impulses = [];
	var effects = [];
	var prevEffects;
	function processEffects() {
	  for (const effect of effects.splice(0)) {
		const [teardown, callback] = effect;
		if (callback) {
		  const param = teardown && execute2(teardown);
		  effect.splice(0, 2, execute2(callback, param), void 0);
		}
	  }
	}
	function onRender(callback, deps) {
	  if (isServer) {
		return;
	  } else if (!callback) {
		return queue.size ? new Promise((resolve) => effects.push([, resolve])) : Promise.resolve();
	  }
	  let effect = prevEffects.shift();
	  if (!effect || deps?.some?.((value, i) => value !== effect[i + 2])) {
		const teardown = effect?.[0];
		effect = [teardown, callback, ...deps];
	  }
	  effects.push(effect);
	}
	function renderImpulse(ref, props, children, context, document, nodes) {
	  if (!ref[1]) {
		ref.splice(1, 2, {}, [, /* @__PURE__ */ new Set(), , ...impulses.slice(0, -1)]);
	  }
	  const [, memo, impulse] = ref;
	  const [parentNode2] = nodes;
	  let prevProxy, prevNodes;
	  const update = () => {
		impulses.unshift(impulse);
		prevEffects = ref.splice(3);
		const effectCount = effects.length;
		const [callback] = ref;
		const nextNodes = [];
		const layout = execute2(callback, { ...props, "": memo }, ...children) || "";
		const proxy = render(layout, context, document, nextNodes, impulse, -1, {});
		if (prevNodes) {
		  reconcile(parentNode2, nextNodes, prevNodes);
		  if (proxy !== prevProxy) {
			remove(prevProxy, parentNode2);
		  }
		}
		impulses.shift();
		ref.push(...effects.slice(effectCount));
		prevProxy = proxy;
		prevNodes = nextNodes;
		return nextNodes;
	  };
	  impulse[0] = update;
	  nodes.push(...update());
	}
  
	// src/index.js
	var defaultDocument = isServer ? virtualDocument : globalThis.document;
	function execute2(callback, ...params) {
	  try {
		return callback?.(...params);
	  } catch (err) {
		console.error(err);
	  }
	}
	function hotSwapStep(ref, manifest, subscriptions) {
	  if (!Array.isArray(ref)) {
		return;
	  }
	  const [callback, , impulse, ...children] = ref;
	  if (!Array.isArray(impulse)) {
		children.map((childRef) => hotSwapStep(childRef, manifest, subscriptions));
		return;
	  }
	  const replacement = manifest.get(callback);
	  if (replacement) {
		ref[0] = replacement;
		subscriptions.add(impulse);
	  }
	  const proxy = impulse[2];
	  hotSwapStep(proxy, manifest, subscriptions);
	}
	function stew(node, context = {}, ...children) {
	  let document = defaultDocument;
	  if (node?.createDocumentFragment) {
		document = node;
		node = document.body;
	  }
	  if (!node) {
		node = document.createDocumentFragment();
	  } else if (typeof node === "string") {
		node = document.querySelector(node);
		if (!node) {
		  return;
		}
	  }
	  const layout = [node, {}, ...children];
	  const ref = render(layout, context, document, [node], ["", {}], 0, {});
	  processEffects();
	  return Object.assign((manifest) => {
		if (!manifest) {
		  return node;
		}
		const subscriptions = /* @__PURE__ */ new Set();
		hotSwapStep(ref, manifest, subscriptions);
		schedule(subscriptions);
	  }, {
		toString: () => String(node)
	  });
	}
  
	// src/main.js
	Object.assign(stew, {
	  createState,
	  onRender,
	  virtualDocument
	});
	if (!isServer) {
	  window.stew = stew;
	}
	if (typeof module === "object") {
	  module.exports = stew;
	}
	var main_default = stew;
  })();
  /*! For license information please see stew.min.js.LEGAL.txt */
  